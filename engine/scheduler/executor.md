# Moved

This document lives at `.claudinite/shared/packs/claudinite-tasks/executor.md` (canon:
`packs/claudinite-tasks/executor.md`). Read and follow it there. This pointer stays only
while fielded routine prompts still name the old path (#1317).
