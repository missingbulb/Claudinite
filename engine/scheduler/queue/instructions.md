# Moved

This document now lives beside the mechanism it belongs to, at
`packs/claudinite-tasks/queue/instructions.md` — in a member's mount,
`.claudinite/shared/packs/claudinite-tasks/queue/instructions.md`.

Read it there and follow it; nothing else remains here. This pointer is deleted once no
stored routine prompt still names this path (#1317).
