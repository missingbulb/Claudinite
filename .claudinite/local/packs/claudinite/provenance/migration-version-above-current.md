## 2026-09-25 · born · Price a new migration record's version against the version its flow still carries
- **Source:** #2300, whose body carried a "Before merge" paragraph saying to raise the record's
  `60924.1` if a bump had already cut that number, re-verified by hand in the session 2h45m later
  when the owner approved.
- **Reason:** a pack's version is bumped after the merge, so a record's `version` predicts somebody
  else's next commit; predicted at or below the standing number the record never vendors and never
  applies, and nothing is red at any point. The hazard's cost is already on record - five members
  lost the executor's `CLAUDINITE_VARS` line to the same out-of-range predicate.
- **Actor:** the growth-extract run over the 2026-09-24 window.
- **Model:** claude-opus-5
- **Mechanism:** a work-scope rule over the change's own records, reading the flow's standing
  version off `pack.mjs` through the registry the runner already loaded, and off `ENGINE_VERSION`
  for an engine record. Advisory, per the growth ladder's rung for hand-written logic no human
  reviewed. Coded rather than declared because dotted-version ordering is a comparison no pattern
  pair can state. Out of scope where the change moves its own flow's version (an engine release -
  `engine-release-record` owns that) or relocates a record, whose id survives what its path does
  not. .claudinite/local/packs/claudinite/workRules/migration-version-above-current.mjs.

## 2026-09-25 · reworded · `severity: blocking|advisory` is spelled `on_fail: block|advise`
- **Reason:** owner decision, 2026-09-25: the field names what happens when the check fails, and
  *severity* keeps its impact sense; what this element enforces is unchanged.
- **Actor:** @missingbulb (owner).
