# Version history

Records for `packs/basics/pack.mjs`'s `version` field, one row per bump — added going forward from
the version this file was introduced beside (60821.4); earlier bumps are not backfilled.

| Version | Date | What changed |
|---|---|---|
| 60822.1 | 2026-08-22 | Growth-promote #1202: a doc-claims-falsified rule beyond privacy/data, two harness-tool-contract entries (a `wait` split across Bash calls, verifying a shell tool exists before polling on it). |
