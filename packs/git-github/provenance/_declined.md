## 2026-09-06 · declined · the git/GitHub candidates kept as prose rather than converted (#1671)
- **Source:** nine growth-promote pull requests consolidated into one reviewed change.
- **Reason:** every candidate with a signature the four-moment check vocabulary can carry rode a
  check in that change; these had none and stayed in the git-github-advanced skill - write scope as
  a policy boundary, amending rather than `git filter-branch`, curl reading an error body as zero
  pending, one wait mechanism, unreachable artifact URLs, a generated file racing a scheduled
  writer, `list_pull_requests`' `merged` field, `issue_read` on a pull request number, subscribing
  to every pull request a sweep leaves open, and `actions_list` ignoring `per_page`.
- **Actor:** @missingbulb (owner).
