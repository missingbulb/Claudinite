## 2026-08-12 · declined · prose considered for the move into this pack, left where it stood (#739)
- **Source:** the consolidation sweep over the canon and the members' local packs.
- **Reason:** the hermetic-input-over-a-live-third-party-target rule is test design, not
  acquisition; bot-blocking is only its motivation, so it stays with writing-tests. A pipeline's
  expected, handled outcomes exiting clean is the general point, of which this pack's dead-end rule
  is the scraping-specific instance: correctly two rules at two altitudes. Growing a corpus from
  public sources is framed around licence, provenance and validation tiers, which is research
  method. The static-site cache-generation rules only use a time-zone correction as an example. The
  rest of `gcec`'s extractor-pipeline section and `edfringe-data`'s wire-format sections are either
  those repos' names for points already homed here or genuinely repo-specific.
- **Actor:** @missingbulb (owner).
- **Model:** Claude Sonnet 5, Claude Opus 5, per the commit trailer.
- **Landed:** #739 (Refs #717) · pack version 1.
