## 2026-09-01 · born · converted from references.md (RULES-1)
- **Reason:** Sophistication is not accuracy: when the guiding signal is noisy or penetrates the
  structure unevenly, a “smarter” local correction can measurably lose to a plain uniform one
  — so the naive baseline is kept as the thing to beat rather than assumed beaten. Recovered from
  the rule's own pre-#467 text (cut by 2f3e4e9a as “consequence prose arguing for a rule rather
  than enabling it”, before this pack had a references.md to hold it).
- **Mechanism:** prose
- **Retire when:** Reaffirm while the method is scored against a baseline; retire if no naive
  baseline is definable for the task.
