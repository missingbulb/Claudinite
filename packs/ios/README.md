# ios pack

Technology **stub** pack: iOS app development — the Xcode project, `Info.plist` usage strings, entitlements, code signing.

## Stub, deliberately

No rules are captured yet. Durable, project-agnostic practices go into `RULES.md` as they are *earned*; the expected first source is `missingbulb/ShoutsAndWhispers`.

The fingerprint is `ios/Runner/Info.plist` at any depth — the Flutter-generated iOS host, which is where the first source will carry it.
