# Version history

Records for `packs/host-page-adaptation/pack.mjs`'s `version` field, one row per bump.

| Version | Date | What changed |
|---|---|---|
| 60823.1 | 2026-08-23 | Promote-to-canon (#99): pack minted from `missingbulb/CrosswordChat`'s local `host-page-adaptation` pack — the judgment core for driving a host web app you don't own, plus its two lifted checks (`page-observers-disconnected`, `synthetic-input-events-bubble`). |
